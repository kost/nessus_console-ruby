require 'spec_helper'

describe NessusConsole do
  it 'has a version number' do
    expect(NessusConsole::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
